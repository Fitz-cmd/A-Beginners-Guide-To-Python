title = 'The Good, The Bad, and the Ugly' 
print('Source string:', title) 
print('Split using a space') 
print(title.split(' '))
print('Split using a comma') 
print(title.split(','))
#A very common requirement is the need to split a string 
# up into multiple separate strings based on a specific character 
# such as a space or a comma.
#This can be done with the split() function, 
# that takes a string to used in identifying 
# how to split up the receiving string. For example: